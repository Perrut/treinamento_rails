<nav class="navbar navbar-inverse">
  <div class="container-fluid">
    <div class="navbar-header">
      <button type="button" class="navbar-toggle" data-toggle="collapse" data-target="#myNavbar">
        <span class="icon-bar"></span>
        <span class="icon-bar"></span>
        <span class="icon-bar"></span>
      </button>
      <%= link_to "BRÓGUI", root_url, class: "navbar-brand" %>
    </div>
    <div class="collapse navbar-collapse" id="myNavbar">
      <ul class="nav navbar-nav">
        <li class="active"><%= link_to "Home", posts_path if logged_in? %></li>
      </ul>
      <ul class="nav navbar-nav navbar-right">
        <% if logged_in? %>
          <li>
            <%= link_to current_user do %>
              Perfil <span class="glyphicon glyphicon-user"></span>
            <% end %>
          </li>
        <% else %>
          <li>
            <%= link_to new_user_path do %>
              Cadastro <span class="glyphicon glyphicon-user"></span>
            <% end %>
          </li>
        <% end %>
        <% if logged_in? %>
          <li>
            <%= link_to logout_path, method: :delete do %>
              Logout <span class="glyphicon glyphicon-log-in"></span>
            <% end %>
          </li>
        <% else %>
          <li>
            <%= link_to about_path do %>
              Sobre <span class="glyphicon glyphicon-road"></span>
            <% end %>
          </li>
        <% end %>
      </ul>
    </div>
  </div>
</nav>
